rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Por ahora, permite todo (SOLO PARA DESARROLLO)
    // ⚠️ IMPORTANTE: Después agregaremos autenticación
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
